# دليل المطور: تشغيل بوت «خدماتي علاوي»

> **قبل البدء:** لا تستخدم رمز البوت السابق مرة أخرى. بما أنه ظهر في ملفات ومحادثات سابقة، ألغِه من `@BotFather` وأنشئ رمزاً جديداً، ثم احفظه كمتغير بيئة فقط. لا تضعه في GitHub أو في `render.yaml`.

## 1. ما الذي تم إنجازه؟

البوت مكتوب ببايثون ويحتوي على القائمة العربية، نظام رصيد SQLite، أمر المطور `/add`، الأوامر العامة، والتحقق من الروابط. تم إصلاح فشل مزود التفاعلات عند إعادة قيمة رقمية، وأصبح الفشل لا يخصم نقاط المستخدم. اجتازت اختبارات المنطق المحلية.

| الجزء | الحالة | ملاحظة تشغيلية |
|---|---|---|
| القوائم والأوامر ونظام الرصيد | جاهز | قاعدة SQLite محلية؛ لا تعتمد عليها للتشغيل طويل الأمد من دون تخزين دائم. |
| طلبات الترويج | جاهز كمسار اختياري | لا تستخدم إلا مزوداً توافق شروطه وتملك تفويضه وواجهة API موثقة. |
| الأرقام | مصدر تجريبي موجود | مواقع العرض العامة غير مستقرة؛ للتشغيل التجاري يلزم مزود API موثق ومفتاح حسابك. |
| جسر Telegram | جاهز | يجب نشره أولاً وتعيين عنوانه في `TELEGRAM_RELAY_URL`. |

## 2. المخطط النصي للتشغيل

```text
[مستخدم Telegram]
        |
        v
[Telegram Bot API] <--> [جسر Telegram المنشور]
        |                         |
        v                         v
[عامل Python على Render] --> [SQLite / مزود API مصرح]
```

## 3. النشر المجاني مقابل التشغيل الدائم

لا يوجد إعداد على Render يضمن تشغيل هذا البوت **24 ساعة مجاناً**. وفق وثائق Render، الخطة المجانية متاحة لخدمات الويب وتدخل السكون بعد 15 دقيقة بلا طلب HTTP، بينما العامل الخلفي المستمر ليس ضمن الأنواع المجانية. لذلك اختر أحد المسارين:

| المسار | الاستخدام | النتيجة |
|---|---|---|
| تجربة مجانية | خدمة Web مؤقتة | غير مناسب لـ `getUpdates` المستمر وقد يفقد ملف SQLite عند السكون أو إعادة النشر. |
| تشغيل مستمر | Background Worker مدفوع أو خادمك الشخصي | مناسب للبوت لأن عملية polling تبقى فعالة. |

لا تستخدم خدمات «إبقاء الموقع مستيقظاً» للتحايل على حدود الخطة؛ فهي لا تعالج مشكلة التخزين المحلي ولا تعطي ضمان تشغيل.

## 4. نشر جسر Telegram

1. افتح مشروع **Telegram API Relay** من مساحة المشروع.
2. افتح آخر نسخة محفوظة ثم اضغط زر **Publish** في الواجهة.
3. انسخ رابط النشر العام، مثل `https://relay.example.manus.space`، من لوحة النشر.
4. احتفظ بالرابط لإضافته إلى متغير `TELEGRAM_RELAY_URL` في الخطوة التالية.

## 5. إعداد Render خطوة بخطوة

```text
[Render Dashboard]
  New +
    -> Background Worker
      -> Connect GitHub repository: services-telegram-bot
        -> Configure
          -> Create Background Worker
```

1. سجّل الدخول إلى [Render](https://dashboard.render.com) عبر GitHub.
2. اختر **New** ثم **Background Worker**. اختر مستودع `services-telegram-bot` والفرع الذي يحتوي على آخر التحديثات.
3. إن طلب Render إعدادات يدوية، استخدم القيم التالية:

| الحقل | القيمة |
|---|---|
| Language | Python 3 |
| Build Command | `pip install -r requirements.txt && playwright install chromium` |
| Start Command | `python bot.py` |
| Root Directory | اتركه فارغاً إذا كانت الملفات في جذر المستودع؛ وإلا حدّد مجلد `tg_bot` |

4. في صفحة **Environment** أضف المتغيرات. اضغط **Add Environment Variable** لكل سطر، ولا تضع القيم في الملف أو في commit.

| المفتاح | القيمة |
|---|---|
| `BOT_TOKEN` | الرمز الجديد الذي أنشأته من BotFather |
| `TELEGRAM_RELAY_URL` | رابط الجسر المنشور، من دون `/bot` في آخره |

5. اضغط **Create Background Worker**. راقب تبويب **Logs** حتى تظهر رسالة بدء التطبيق، ثم أرسل `/start` إلى البوت.
6. عند تعديل الكود ورفعه إلى GitHub، نفّذ **Manual Deploy → Deploy latest commit** أو فعّل النشر التلقائي.

## 6. التشغيل المحلي للمطور

```bash
git clone https://github.com/Aaaaaaq08pu/services-telegram-bot.git
cd services-telegram-bot
python -m venv .venv
source .venv/bin/activate       # Windows: .venv\Scripts\activate
pip install -r requirements.txt
playwright install chromium
export BOT_TOKEN="ضع-الرمز-الجديد-هنا"
export TELEGRAM_RELAY_URL="https://رابط-الجسر-المنشور"
python bot.py
```

شغّل نسخة واحدة فقط من البوت بالرمز نفسه. ظهور `409 Conflict` يعني أن عملية أخرى تنفذ `getUpdates` بالرمز ذاته؛ أوقفها قبل إعادة التشغيل.

## 7. الاختبار قبل النشر

```bash
python -m unittest -v test_validation.py test_conversation_flows.py test_buzt_response.py
```

النتيجة المتوقعة هي نجاح جميع الاختبارات. هذه الاختبارات لا ترسل طلبات Telegram فعلية ولا تخصم رصيداً.

## 8. إعداد مزود الأرقام بطريقة موثقة

لا تدمج بوتات خارجية من روابطها فقط ولا تستخدم مواقع عرض الرسائل العامة لخدمة مدفوعة. اختر مزوداً يقدّم API رسمياً، أنشئ حساباً ومفتاح API، ثم أضف المفتاح في متغير بيئة مثل `SMS_MAN_API_KEY`. بعد استلام المفتاح يمكن إضافة التكامل بحيث يتم حجز الرقم أولاً، ثم خصم رصيد مستخدم البوت **بعد نجاح الحجز فقط**.

## 9. استكشاف الأعطال

| العرض | السبب المرجح | الإجراء |
|---|---|---|
| `409 Conflict` | نسختان من البوت تستقبلان التحديثات | أوقف النسخة الأخرى ثم أعد نشر نسخة واحدة. |
| أخطاء اتصال Telegram | لم يُعيّن عنوان الجسر أو لم يُنشر | تحقق من `TELEGRAM_RELAY_URL` ومن صفحة حالة الجسر. |
| اختفاء أرصدة المستخدمين | SQLite على نظام ملفات مؤقت | انقل البيانات إلى قاعدة خارجية قبل الاعتماد التشغيلي. |
| فشل مصدر الأرقام | موقع عام محجوب أو تغيّر تصميمه | استخدم API موثقاً بدلاً من Web scraping. |

## المراجع

1. [Render — Deploy for Free](https://render.com/docs/free)
2. [Render — Service Types](https://render.com/docs/service-types)
3. [Render — Background Workers](https://render.com/docs/background-workers)
